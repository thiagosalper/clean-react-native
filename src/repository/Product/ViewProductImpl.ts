import Product from "../../data/models/Product";
import { ViewProduct } from "../../data/ViewProduct";

const ViewProductImpl: ViewProduct = (productId) => {
  return {} as Product;
}

export default ViewProductImpl;
