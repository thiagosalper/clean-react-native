import { Cart } from "../models";
import { InsertProduct } from "../protocols/InsertProduct";

const InsertProductImpl: InsertProduct = (user, product) => {
  return {} as Cart;
}

export default InsertProductImpl;
